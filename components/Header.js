import Link from 'next/link';

const linkStyle = {
    marginRight: 15
};

const Header = () => (
    <header>
        <h1>Simple Plain Initiative Tracker</h1>
    </header>
);

export default Header;